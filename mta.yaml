ID: snake-game
_schema-version: "3.1"
version: 1.0.0
modules:
  - name: snake-game
    type: html5
    path: .
    parameters:
      memory: 64M
      disk-quota: 256M
      buildpack: staticfile_buildpack
      routes:
        - route: snake-game.cfapps.ap21.hana.ondemand.com
    build-parameters:
      builder: custom
      commands: []
      build-result: .
      ignore:
        - "mta.yaml"
        - "Jenkinsfile"
        - ".git/"
        - ".gitignore"
resources: []
